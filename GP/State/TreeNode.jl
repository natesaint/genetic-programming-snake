########################################
# Represent a node in a tree structure.
#

abstract type TreeNode end

# Recursively, parse the tree into a function structure that can be called
function parseTree(root::TreeNode)
end
